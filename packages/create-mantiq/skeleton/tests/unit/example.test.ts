import { describe, test, expect } from 'bun:test'

describe('Example', () => {
  test('true is truthy', () => {
    expect(true).toBe(true)
  })

  test('math works', () => {
    expect(1 + 1).toBe(2)
  })
})
